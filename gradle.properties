# Project-wide Gradle settings.

# Enable modern AndroidX architecture
android.useAndroidX=true

# Automatically convert older third-party libraries to AndroidX if needed
android.enableJetifier=true

# Give the Gradle daemon enough memory to build smoothly in the cloud
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
